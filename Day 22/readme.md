Solved the Search Insert Position problem, used the same trick as Binary Search but instead of returning -1 when the lb crossed ub, just returned the lb.
The final algorithm resulted in a time complexity of O(log n) and space complexity of O(1).
It has a runtime of 0ms and 13.61 MB of memory.