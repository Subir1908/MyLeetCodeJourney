Solved the Is Subsequence problem where we we have to check if the first string is the subset of the second string and also in same order/sequence.
The final algorithm has a time complexity of O(N) and space complexity of O(1).
The initial algorithm had a runtime of 0ms and 8.66 MB.
Checked the code and again and then replaced the i and j with s.size() and t.size() so i an j woudn't use more memory.
The final algorithm has a runtime of 0ms and 8.58 memory.