Solved the Reverse vowels in a string problem. Used two pointer one from the start and one from the end of the string and looped until both of them landed on a vowels and when they did the program made the swap and so on.
The final algorithm has the time complexity of O(N) and space complexity of O(1).
It has a runtime of 0ms and memory of 10.30 MB.