Solved the Baseball Game problem where we have to perform different operations based on the ooperations string input while mantaining a stack.
Used stack for the solution. 
The final algorithm has a time complexity of O(N) and space complexity of O(N) which is optimal for the porblem.
It has a runtime of 0ms and 12.72 MB memory.