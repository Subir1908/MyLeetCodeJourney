Solved the Binary Search problem where we have to search for an item inside a list using binary search.
Used two pointer strategy to solve the problem, One for lower bound and one for Upper bound and a variable to store the middle value.
The final algorithm has a runtime of 0ms and 31.22 MB of memory. 
The time complexity of the algorithm is O(log N) which is optimal and the space complexity is O(1).