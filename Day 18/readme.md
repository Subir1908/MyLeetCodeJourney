Solved the Find the Index of the First Occurrence in a String problem.
Used two pointer method for the solution and the worst time complexity of the algorithm is O(N * M) and space complexity is O(1).
The final algoritm took 1ms of runtime and 9.22 MB of memory.