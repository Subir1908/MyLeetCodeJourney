Completed the Longest Common prefix problem where we have to find the longest common prefix from a set of strings by checking each letter one by one from the start.
Used two loop one for the length of the string container and one for the size of each of the first string.
The program ran into some errors initially but was resolved later on.
The final algorithm ran in 0ms time and uses 11.83 MB of memory. 
It has a time complexity of O(S), where S stands for the total number of characters checkhed and space complexity of O(1).