Solved the Two Sum II - Input Array is sorted problem where the array given to find the two sum is sorted in increasing order.
Used two pointer technique to use two values from the array and then adjust the pointers according to the comparison with the target value.
The final algoritm has a time complexity of O(N) and a space complexity of O(1).
It has a runtime of 0ms and a 19.60 MB memory.